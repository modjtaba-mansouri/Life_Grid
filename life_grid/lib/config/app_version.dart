/// Auto-overwritten by the CI workflow at build time to match the
/// version in pubspec.yaml. The placeholder here is only used if you
/// run the app straight from this source without going through CI.
const String kAppVersion = '2.7.4';
